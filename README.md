1. copy `config/kcp.json.example config/kcp.json` and `config/shadowsocks.json.example config/shadowsocks.json`. 


3. build docker image

  `sudo docker-compose build`
  
4. start container

  `sudo docker-compose up -d`
